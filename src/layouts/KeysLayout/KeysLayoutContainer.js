import { connect } from 'react-redux'

import KeysLayout from './KeysLayout'

const mapStateToProps = () => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(KeysLayout)
